---
import profileIcon from "../assets/profile_icon.jpg";
import KeywordsSection from "./KeywordsSection.astro";
import ProfileBio from "./ProfileBio.astro";
import SocialIcons from "./SocialIcons.astro";
// Define bio entries
const bioEntries = [
  "静岡県浜松市出身。浜松北高卒業後。",
  "東京大学文科二類に入学後 IT に興味を持ち、理転し工学部電子情報工学科に進学。工学部長賞受賞。",
  "Strobo（インターン）, Microsoft, FLYWHEEL でエンジニアを経験後、picon に入社。",
  "新規事業を中心にフロントエンドやバックエンド、機械学習まで幅広く担当。",
];

// Define keywords
const keywords = [
  { icon: "mdi:brain", text: "AI" },
  { icon: "mdi:music", text: "音" },
  { icon: "mdi:language", text: "言語" },
  { icon: "mdi:school", text: "教育" },
  { icon: "mdi:human-greeting-proximity", text: "つながり" },
];
---
<div class="card-bg">
  <div class="md:flex">
    <div class="md:w-1/3 card-padding flex flex-col items-center justify-center bg-gradient-to-b from-white/20 to-transparent">
      <div class="relative">
        <img
          src={profileIcon.src}
          alt="Profile icon"
          class="w-28 h-28 sm:w-32 sm:h-32 md:w-36 md:h-36 rounded-full object-cover border-2 border-accent/10 shadow-md"
        />
        <div class="absolute inset-0 rounded-full border-2 border-accent/5 transform scale-110 animate-pulse"></div>
      </div>

      <div class="mt-6 md:mt-8 text-center">
        <h3 class="text-responsive-xl font-light text-accent mb-1">大村 瞬也</h3>
        <p class="text-text/50 font-light tracking-wide">OHMURA Tokiya</p>
        <p class="text-text/50 text-responsive-sm mb-4 md:mb-6 font-light tracking-wide">Senior Software Engineer / picon inc.</p>

        <SocialIcons />
      </div>
    </div>

    <div class="md:w-2/3 card-padding flex flex-col">
      <div class="prose max-w-none flex-grow">
        {bioEntries.map((entry) => (
          <ProfileBio text={entry} />
        ))}
      </div>

      <div class="mt-4 pt-4 border-t border-accent/5">
        <KeywordsSection keywords={keywords} />
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes pulse {
    0%, 100% {
      opacity: 0.2;
    }
    50% {
      opacity: 0.5;
    }
  }

  .animate-pulse {
    animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
</style>
