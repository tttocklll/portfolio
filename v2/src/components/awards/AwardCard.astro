---
import { Icon } from "astro-icon/components";

export interface Props {
  title: string;
  badge?: string;
  date: string;
  description: string;
  link?: string;
}

const { title, badge, date, description, link } = Astro.props;
---

<div class="card-bg">
  <div class="card-padding">
    <div class="flex justify-between items-start mb-3 sm:mb-4">
      <h3 class="text-responsive-base font-medium text-accent">
        {link ? (
          <a href={link} class="hover:underline items-center" target="_blank" rel="noopener noreferrer">
            {title}
            <Icon name="mdi:open-in-new" class="inline-block icon-responsive-base" />
          </a>
        ) :
          title
        }
      </h3>
      {badge && (
        <span class="text-responsive-sm font-light bg-accent/10 text-accent px-1.5 sm:px-2 py-0.5 sm:py-1 rounded-full">{badge}</span>
      )}
    </div>
    <time class="block mb-2 sm:mb-3 text-responsive-sm font-light text-text/60">{date}</time>
    <p class="text-text/80 text-responsive-sm">
      {description}
    </p>
  </div>
</div>
