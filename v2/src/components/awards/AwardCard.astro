---
import { Icon } from "astro-icon/components";

export interface Props {
  title: string;
  badge?: string;
  date: string;
  description: string;
  link?: string;
}

const { title, badge, date, description, link } = Astro.props;
---

<div class="card-bg">
  <div class="p-4 sm:p-6">
    <div class="flex justify-between items-start mb-3 sm:mb-4">
      <h3 class="text-base sm:text-lg font-medium text-accent">
        {link ? (
          <a href={link} class="hover:underline items-center" target="_blank" rel="noopener noreferrer">
            {title}
            <Icon name="mdi:open-in-new" class="inline-block w-3.5 h-3.5 sm:w-4 sm:h-4" />
          </a>
        ) :
          title
        }
      </h3>
      {badge && (
        <span class="text-xs font-light bg-accent/10 text-accent px-1.5 sm:px-2 py-0.5 sm:py-1 rounded-full">{badge}</span>
      )}
    </div>
    <time class="block mb-2 sm:mb-3 text-xs sm:text-sm font-light text-text/60">{date}</time>
    <p class="text-text/80 text-xs sm:text-sm">
      {description}
    </p>
  </div>
</div>
